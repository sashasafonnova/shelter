const App: React.FC = () => {

    const TestComponent = () => {
        return (
            <h1 className="app">Привет, мир!</h1>
        );
    };

    return (
        <TestComponent />
    );
};

export default App;